{
  "name": "url-shortner",
  "version": "1.0.0",
  "description": "Simple Url shortner",
  "main": "src/index.ts",
  "scripts": {
    "server-build": "cd ./server && yarn build",
    "server-start": "cd ./server && yarn start",
    "server-dev": "cd ./server && yarn run dev",
    "server-install": "cd ./server && yarn",
    "install": "yarn run server-install",
    "server-test": "cd ./server && yarn run test",
    "test": "yarn run server-test"
  },
  "dependencies": {
    "get-urls": "^10.0.0",
    "node-fetch": "^2.6.1"
  },
  "devDependencies": {
    "@types/node-fetch": "^2.5.12"
  }
}
